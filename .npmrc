# Use pnpm for package management
# This ensures consistent behavior across the monorepo
engine-strict=true

# Security: Prevent same-day supply chain attacks
# Only install packages that are at least 3 days old
minimum-release-age=259200000
